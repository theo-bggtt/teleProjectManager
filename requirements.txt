python-telegram-bot>=21.0,<22
APScheduler>=3.10,<4
# System healthcheck (admin menu):
psutil>=5.9
# Trading module (optional — only needed when [trading] is enabled):
aiohttp>=3.9
websockets>=12.0

# Portfolio chart rendering:
matplotlib>=3.7

# Dev (tests)
pytest>=8.0
pytest-asyncio>=0.23
